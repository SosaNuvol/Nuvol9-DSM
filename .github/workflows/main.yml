name: .NET Core CI

on:
  push:
    branches: [DEV]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up .NET
      uses: actions/setup-dotnet@v2
      with:
        dotnet-version: '8.0' # or your project's .NET version

    - name: Restore dependencies
      run: dotnet restore NVL9.DSM.WebAPITest.csproj

    - name: Build
      run: dotnet build NVL9.DSM.WebAPITest.csproj --configuration Release --no-restore

    - name: Test
      run: dotnet test NVL9.DSM.WebAPITest.csproj --no-restore --verbosity normal

    - name: Publish
      run: dotnet publish NVL9.DSM.WebAPITest.csproj --configuration Release --output ./output